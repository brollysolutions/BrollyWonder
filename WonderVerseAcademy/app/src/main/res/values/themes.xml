<resources>
    <!--
      Edge-to-edge is opted into programmatically via enableEdgeToEdge() in
      MainActivity. Do not add an "android:windowOptInEdgeToEdge" item here —
      no such platform attribute exists (the real API 35 attribute is
      android:windowOptOutEdgeToEdgeEnforcement), and it breaks resource linking.
    -->
    <style name="Theme.WonderVerse" parent="Theme.MaterialComponents.DayNight.NoActionBar" />
</resources>
